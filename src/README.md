# Source layout

O código-fonte principal está em `../EcoTrack360` e os testes automatizados em `../EcoTrack360.Tests`.

Esta pasta existe para atender ao checklist de entrega que solicita uma estrutura mínima com `src/`.

Para a execução local com Docker, copie `.env.example` para `.env` antes de subir os containers.